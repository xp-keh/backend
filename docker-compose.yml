services:
  backend:
    build: .
    ports:
      - "4000:3000"
    env_file:
      - .env
    command: npm start
    networks:
      data-station-network:
        ipv4_address: 172.20.14.91

networks:
  data-station-network:
    external: true
