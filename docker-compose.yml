services:
  backend:
    build: .
    ports:
      - "4000:4000"  # Corrected port mapping
    networks:
      data-station-network:
        ipv4_address: 172.20.14.91

networks:
  data-station-network:
    external: true